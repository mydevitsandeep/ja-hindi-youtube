"use strict"; // treat all JS code as newer version

// alert(3+3) // we are using nodeJS not browser and alert not work like this in NodeJS

let age = 18;
let loggedIn = "sandeep";
let nameFirst = true;

console.log(typeof age);
console.log(typeof loggedIn);
console.log(typeof nameFirst);

